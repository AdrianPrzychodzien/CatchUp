<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <section class="relative pt-16 pb-0 bg-white bg-pattern-white bg-center min-h-screen md:flex md:items-center">
      <div class="container px-4 mx-auto mb-16 flex flex-col justify-center items-center">
        <div class="w-full md:w-3/5 lg:w-1/2">
          <div class="max-w-sm mx-auto">
            <div class="mb-6 text-center">
              <h3 class="mb-4 text-2xl md:text-3xl font-bold"><%= @title %></h3>
            </div>
            <%= yield %>
          </div>
        </div>
      </div>
    </section>
  </body>
</html>
