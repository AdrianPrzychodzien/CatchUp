<%= render 'admin/shared/header', resource: @deck, mode: "show" %>

<%= render 'admin/shared/details', resource: @deck, select_columns: @select_columns %> 

<%= button_to 'Download in anki format', download_teacher_deck_path, data: {turbo: "false"}, class:"btn-primary" %>
