<div class="my-2 border-2">
  Your students:
  <ul>
    <% @students.each do |student| %>
      <li>
        <%= link_to student.email, teacher_student_path(student) %>
      </li>
    <% end %>
  </ul>
</div>
